# Docs & API Conventions

## Overview

Documentation and example standards for the public API.

## Rules

- Prefer `createAdapter` in examples and tests.
- Keep JSON as the default serialization with override hooks.
- When adapter behavior changes, update `README.md` and tests together.
